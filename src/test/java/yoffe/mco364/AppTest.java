package yoffe.mco364;

import static org.junit.Assert.assertTrue;
import org.junit.Test;

/**
 * Unit test for simple App.
 */
public class AppTest {	
	@Test
	public void testApp() {
		assertTrue(true);
	}

	/* hamcrest is a dependency of a dependency(junit 4.12)
	 * difference bet junit 3 and 4 is that junit 4 has annotations
	 * 
	 * don't need to deal with jar files and junit
	 *  
	 *  */

}
